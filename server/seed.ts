import "./db"; // ensure Firebase is initialized

export async function seedDatabase() {
  console.log("Firestore ready. No seed data - all data comes from real wallet connections and blockchain operations.");
}
